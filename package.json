{
  "name": "bundles-prediction-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ bundles/bundles-prediction-subgraph",
    "create-local": "graph create --node http://localhost:8020/ bundles/bundles-prediction-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ bundles/bundles-prediction-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 bundles/bundles-prediction-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.1",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
